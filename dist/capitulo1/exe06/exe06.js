"use strict";
// Crie um array chamado cores com 3 strings. Imprima cada cor usando um for...of.
function imprimirCores(cores) {
    for (const cor of cores) {
        console.log(cor);
    }
}
const cores = ["vermelo", "verde", "azul"];
imprimirCores(cores);
