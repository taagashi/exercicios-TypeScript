// Crie uma função chamada calcularAreaRetangulo que receba largura e altura e retorne a área (largura * altura).

function calcularAreaRetangulo(largura: number, altura: number) {
    return (largura * altura);
}

const areaCalculada = calcularAreaRetangulo(12, 5);

console.log(areaCalculada);